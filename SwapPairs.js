/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {ListNode}
 */
var swapPairs = function(head) {

};

//each node val, next:


//check if the current node had a next value
  //if not, then return that node’s value
  //if the node’s value is null, return empty node

//create

//if the node has a next value
  //swap the current node with the next node
//move forward in iteration by two

Node = [ val: , next: ]

Const swapNodePairs = (head) => {

If(!node.value){
 Return undefined
}
If(node.next === ‘undefined’){
Return {val: val, next=null}
}

 While( node.next){
   currentNode = { vale, next}
   nextNode={val: , next}

   var a = currentNode.val
  var b = nextNode.val

currentNode.val = b
nextNode.val= a



}




}
